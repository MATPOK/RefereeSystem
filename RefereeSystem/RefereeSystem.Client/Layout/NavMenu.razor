@using RefereeSystem.Client.Services
@using Blazored.LocalStorage
@using Microsoft.AspNetCore.Components.Authorization
@inject AuthenticationStateProvider AuthStateProvider
@inject ILocalStorageService LocalStorage
@inject NavigationManager NavManager

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">RefereeSystem</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <AuthorizeView>
            <Authorized>

                <AuthorizeView Roles="Admin,Scheduler" Context="managementContext">
                    <div class="nav-item px-3">
                        <NavLink class="nav-link" href="matches">
                            <span class="bi bi-calendar-event-nav-menu" aria-hidden="true"></span> Mecze
                        </NavLink>
                    </div>
                </AuthorizeView>
                <div class="nav-item px-3">
                    <span class="text-light small p-2">Zalogowany jako: @context.User.Identity?.Name</span>
                </div>

                <div class="nav-item px-3">
                    <button class="nav-link btn btn-link text-white" @onclick="Logout">
                        <span class="bi bi-box-arrow-right-nav-menu" aria-hidden="true"></span> Wyloguj
                    </button>
                </div>

                <AuthorizeView Roles="Admin" Context="adminContext">
                    <div class="nav-item px-3">
                        <NavLink class="nav-link" href="users">
                            <span class="bi bi-people-nav-menu" aria-hidden="true"></span> Użytkownicy
                        </NavLink>
                    </div>
                </AuthorizeView>

            </Authorized>

            <NotAuthorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="login">
                        <span class="bi bi-person-badge-nav-menu" aria-hidden="true"></span> Zaloguj
                    </NavLink>
                </div>
            </NotAuthorized>
        </AuthorizeView>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    private async Task Logout()
    {
        // 1. Usuń token
        await LocalStorage.RemoveItemAsync("authToken");

        // 2. Powiadom system o wylogowaniu
        (AuthStateProvider as CustomAuthStateProvider).NotifyUserLogout();

        // 3. Przekieruj
        NavManager.NavigateTo("/");
    }
}