@page "/"

<PageTitle>Home</PageTitle>

<h1>Witaj w systemie!</h1>

<AuthorizeView>
    <Authorized>
        @if (context.User.IsInRole("Guest"))
        {
            <div class="alert alert-warning mt-4">
                <h4>Twoje konto oczekuje na akceptację.</h4>
                <p>Zalogowałeś się pomyślnie, ale nie masz jeszcze uprawnień. Skontaktuj się z administratorem, aby nadał Ci rolę Sędziego lub Obsadowcy.</p>
            </div>
        }
        else
        {
            <p>Jesteś zalogowany. Wybierz opcję z menu.</p>
        }
    </Authorized>
    <NotAuthorized>
        <p>Zaloguj się, aby korzystać z systemu.</p>
    </NotAuthorized>
</AuthorizeView>